if ('clipboard' in navigator) {

  // Escrevo na area de transferencia(clipboard)
  // navigator.clipboard.writeText('Jimmy Codder');

  // Lendo do clipboard
  console.log(navigator.clipboard.readText());
}